# BACKEND_TODO.md

## Phase 1: Environment & Foundation
- [ ] Fix `requirements.txt` (add `fastapi`, `psycopg2-binary`, `python-jose[cryptography]`).
- [ ] Remove `pyjwt` from `requirements.txt` (conflicts with `python-jose`).
- [ ] Create `.env.example`.
- [ ] Remove `print(settings.DATABASE_URL)` from `backend/app/core/database.py`.
- [ ] Add `JWT_ALGORITHM` and `ACCESS_TOKEN_EXPIRE_MINUTES` to `config.py`.
- [ ] Configure CORS middleware in `main.py` to allow `localhost:3000`.
- [ ] Register `health` router in `main.py` and verify `uvicorn` starts successfully.
**Definition of Done:** The FastAPI app runs locally without crashing, and `/docs` is accessible.

## Phase 2: Authentication & Identity
- [ ] Write `get_current_user` dependency in `app/core/auth.py` to decode Supabase JWT using `SUPABASE_JWT_SECRET`.
- [ ] Remove legacy password hashing logic from `auth_service.py`.
- [ ] Update `models/user.py` to remove `password_hash` (matching Supabase migration 0057).
- [ ] Implement `GET /api/v1/auth/me` to return the verified user profile.
- [ ] Add tenant-isolation context variable (e.g., `current_org_id = contextvars.ContextVar("current_org_id")`).
**Definition of Done:** The frontend can send a Supabase JWT in the `Authorization` header, and the backend correctly identifies the user and their organization.

## Phase 3: Core Database Models (SQLAlchemy)
- [ ] Ensure `models/organization.py` accurately reflects the Supabase schema.
- [ ] Delete duplicate `AIMessage` class from `ai_memory.py`.
- [ ] Write missing models: `AIConversation`, `AIMessage`, `AIMemory`.
- [ ] Write missing schemas: `crm.py`, `invoice.py`.
- [ ] Implement `organizations` router to allow self-service org creation.
**Definition of Done:** SQLAlchemy can read/write organizations and users without schema mismatch errors.

## Phase 4: AI Chat MVP (Vertical Slice)
- [ ] Implement `backend/app/ai/model_router.py` (OpenRouter API client).
- [ ] Implement `backend/app/ai/engine.py` (Core prompt construction and LLM calling).
- [ ] Write `backend/app/ai/agents/sales_agent.py` (System prompt wrapper).
- [ ] Implement `POST /api/v1/ai-chat/conversations` (Create chat session).
- [ ] Implement `POST /api/v1/ai-chat/messages` (Stream AI response).
**Definition of Done:** The frontend chat interface can send a message to the backend and receive a streamed AI response generated by OpenRouter.

## Phase 5: CRM Domain (Data for AI)
- [ ] Write models: `Customer`, `Lead`, `Pipeline`.
- [ ] Write schemas for CRM data.
- [ ] Implement `GET/POST /api/v1/crm/customers`.
- [ ] Implement `GET/POST /api/v1/crm/leads`.
- [ ] Update `sales_agent.py` with a tool (`search_crm`) allowing it to fetch leads.
**Definition of Done:** The user can view CRM data in the dashboard, and the AI Sales Agent can query CRM data to answer questions.

## Phase 6: Background Workers (Celery)
- [ ] Configure `celery_app.py` with Redis broker URL.
- [ ] Write `email_worker.py` for sending transactional emails.
- [ ] Write `embedding_worker.py` to chunk documents and store them via pgvector.
**Definition of Done:** Heavy tasks are offloaded to Celery, keeping API response times low.

## Phase 7: Production Readiness
- [ ] Add global exception handlers in `main.py` (return clean JSON instead of stack traces).
- [ ] Add request logging middleware.
- [ ] Set up Dockerfile for the FastAPI app.
- [ ] Set up `docker-compose.yml` for API, Celery Worker, and Redis.
**Definition of Done:** The backend can be deployed to a production environment using Docker.
